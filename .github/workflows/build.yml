name: Evolution System v5.5 - Clean Deployment Pipeline P2P

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-windows:
    name: Compilation Target - Windows x64 v5.5 (.exe)
    runs-on: windows-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Initialize Production Python Environment
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Compiling Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller psutil

      - name: Execute PyInstaller Production Build
        run: |
          pyinstaller --clean --onefile --name="evolution_v5.5_final" evolution_core.py

      - name: Upload Windows Production Binary Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Evolution_v5.5_Windows_x64
          path: dist/evolution_v5.5_final.exe
          retention-days: 5

  build-linux:
    name: Compilation Target - Linux x64 v5.5 (Binary)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Initialize Production Python Environment
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Compiling Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller psutil

      - name: Execute PyInstaller Linux Build
        run: |
          pyinstaller --clean --onefile --name="evolution_v5.5_linux_final" evolution_core.py

      - name: Upload Linux Production Binary Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Evolution_v5.5_Linux_x64
          path: dist/evolution_v5.5_linux_final
          retention-days: 5
